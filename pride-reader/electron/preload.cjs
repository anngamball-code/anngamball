// Preload mínimo: la app es 100% web (no necesita APIs nativas expuestas).
// Se mantiene por seguridad con contextIsolation activado.
window.addEventListener("DOMContentLoaded", () => {
  // Espacio reservado por si en el futuro se exponen APIs nativas seguras.
});
